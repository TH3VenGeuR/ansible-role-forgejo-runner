[Unit]
Description=Forgejo runner
After=syslog.target network.target

[Service]
Type=simple
User=debian
Group=debian
WorkingDirectory=/srv/forgejorunner
ExecStart=/bin/forgejo-runner --config runner-config.yaml daemon

[Install]
WantedBy=multi-user.target

